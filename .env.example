# Path to your security config (yaml). Defaults to ./security.config.yaml
SECURITY_MCP_CONFIG=./security.config.yaml

# Example secrets referenced from config via ${VAR_NAME}
# POLICY_API_TOKEN=
# PAVED_ROAD_API_TOKEN=
# EXCEPTION_API_TOKEN=
